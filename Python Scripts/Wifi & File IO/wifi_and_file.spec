# -*- mode: python -*-

block_cipher = None


a = Analysis(['wifi_and_file.py'],
             pathex=['C:\\Users\\medgar\\Documents\\GitHub\\Wireless_Controller_CC3200\\Python Scripts\\Wifi & File IO'],
             binaries=[],
             datas=[],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='wifi_and_file',
          debug=False,
          strip=False,
          upx=True,
          console=False )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=False,
               upx=True,
               name='wifi_and_file')
